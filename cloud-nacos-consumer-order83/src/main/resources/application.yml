server:
  port: 83

spring:
  application:
    name: cloud-nacos-consumer #指定服务名称
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #配置Nacos地址

# 消费者调用提供者的服务名称(成功注册进nacos的服务名称)
service-url:
  nacos-user-service: http://nacos-payment-provider #nacos-provider-payment